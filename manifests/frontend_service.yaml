apiVersion: v1
kind: Service
metadata:
  name: ${app_name}-frontend-service
  namespace: ${app_name}
  labels:
    app: ${app_name}
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3002
  selector:
    app: ${app_name}
